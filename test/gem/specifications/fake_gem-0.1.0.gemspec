# frozen_string_literal: true
# stub: fake_gem 0.1.0 ruby lib
# stub:

spec = Gem::Specification.new
spec.name = "fake_gem"
spec.version = "0.1.0"
spec.summary = "fake gem"
spec.authors = ["Cora"]
spec.files = ["lib/fake_gem.rb"]
spec.require_paths = ["lib"]
spec
