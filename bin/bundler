#!/bin/sh

bindir=$(cd "${0%/*}" && pwd -P)
cora="${bindir}/../build/bin/cora"
bundle="${bindir}/../build/ext/rubygems/bundler/exe/bundle"

if [ ! -x "${cora}" ] || [ ! -f "${bundle}" ]; then
  echo "Cora or Bundler not found; run \`zig build\`" >&2
  exit 1
fi

exec "${cora}" "${bundle}" "$@"
